{
  "name": "npm-test",
  "version": "1.0.0",
  "description": "this is my first backend project",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "app.js",
  "_comment": "npm support scripts so we can write differnt scripts for different works for start script or some other main key word we have to use npm start for other we have to use npn run ----- in the terminal",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "khul-ja-sim-sim": "node app.js"
  },
  "devDependencies": {
    "nodemon": "^3.1.14" 
  }
}
